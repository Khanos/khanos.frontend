---
import {setDefaultLang} from '../../i18n/ui';

export function getStaticPaths() {
  return [
    { params: { lang: 'es' } },
    { params: { lang: 'en' } },
  ];
}
const { lang } : any = Astro.params;
setDefaultLang(lang);
return Astro.redirect('/');
---