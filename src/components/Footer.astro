---
import { getLang, useTranslations } from '../i18n/utils';

type tLang = (str: any) => any;

const lang = getLang();
const t:tLang = useTranslations(lang);
const currentYear = new Date().getFullYear()
---

<footer
  class="rounded-lg shadow m-4 bg-black/5 dark:bg-black/20 backdrop-blur-lg w-full mx-auto container lg:max-w-4xl md:max-w-2xl mb-10 flex justify-center"
>
  <div
    class="w-full max-w-screen-xl mx-auto md:flex md:items-center md:justify-center"
  >
    <span class="text-sm text-center text-zinc-800/90 dark:text-zinc-200/90 ml-3"
      >© {currentYear}
      <a href="https://epilef.org/" class="hover:underline">khanos</a>. {t('footer').copy}.
    </span>
  </div>
</footer>